spring:
  datasource:
    url: jdbc:mysql://localhost:3306/scrap_treasure
    username: root
    password: student12345

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 8080
